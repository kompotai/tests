# ============================================
# Kompot E2E Tests — Environment Variables
# ============================================
#
# Два режима работы:
#
# 1. TESTER MODE (для тестировщиков)
#    - Нет MONGODB_URI
#    - Только UI тесты
#
# 2. CI MODE (для разработчиков и GitHub Actions)
#    - Есть MONGODB_URI
#    - Полный тест-сьют
#
# ============================================

# --------------------------------------------
# TESTER MODE
# --------------------------------------------

BASE_URL=https://stage.kompot.ai
WS_ID=ваш-workspace-id
WS_OWNER_EMAIL=your-email@example.com
WS_OWNER_PASSWORD=your-password

# HTTP Basic Auth для Stage (выдаётся тестировщикам персонально)
# STAGE_HTTP_USER=kompot
# STAGE_HTTP_PASSWORD=выдаётся-персонально

# --------------------------------------------
# CI MODE (добавьте к переменным выше)
# --------------------------------------------

# MONGODB_URI=mongodb://kompot:kompot@localhost:27017/?authSource=admin
# SUPER_ADMIN_EMAIL=admin@example.com
# SUPER_ADMIN_PASSWORD=xxx
